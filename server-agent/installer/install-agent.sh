#!/usr/bin/env sh
set -eu

echo "MOXI Server Agent installer placeholder"
echo "P0 server-agent is implemented as a Node outbound heartbeat reporter."
echo "It must not contain production secrets or customer-specific tokens."
echo "Configure BAIRUI_PLATFORM_HEARTBEAT_URL and BAIRUI_SERVER_AGENT_TOKEN outside Git."
